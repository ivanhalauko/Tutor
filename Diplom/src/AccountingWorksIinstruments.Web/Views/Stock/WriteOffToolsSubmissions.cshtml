@model IEnumerable<AccountingWorkInstruments.DataAccess.Models.SubmissionWriteTool>

@{
    ViewData["Title"] = "WriteOffToolsSubmissions";
}

<h1>WriteOffToolsSubmissions</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ToolId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubmissionWriteID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ToolId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubmissionWriteID)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
