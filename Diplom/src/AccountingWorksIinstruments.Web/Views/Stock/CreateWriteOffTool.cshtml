@model IEnumerable<AccountingWorksIinstruments.Web.Models.ToolViewModel>

@{
    ViewData["Title"] = "CreateWriteOffTool";
}

<h1>CreateWriteOffTool</h1>

<p>
    <button style="btn-light">
        <a asp-action="WriteOffToolsSubmissions">Write off tools submissions</a>
    </button>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StatusDiscription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PosterImageUrl)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            @if (item.MarkWriteOffTools == true)
            {
                <tr style="background-color:burlywood; color:slategray">
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.StatusDiscription)
                    </td>
                    <td>
                        <img src="@item.PosterImageUrl" style="width:100px" />
                    </td>
                    <td>
                        @Html.ActionLink("Add to List", "AddToWriteOffList", new { toolId = item.Id }) |
                    </td>
                </tr>
            }
            else
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.StatusDiscription)
                    </td>
                    <td>
                        <img src="@item.PosterImageUrl" style="width:100px" />
                    </td>
                    <td>
                        @Html.ActionLink("Add to List", "AddToWriteOffList", new { toolId = item.Id }) |
                    </td>
                </tr>
            }

        }
    </tbody>
</table>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StatusDiscription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PosterImageUrl)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            @if (item.MarkWriteOffTools == true)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.StatusDiscription)
                    </td>
                    <td>
                        <img src="@item.PosterImageUrl" style="width:100px" />
                    </td>
                    <td>
                        @Html.ActionLink("Remove", "RemoveFromWriteOffSubmission", new { toolId = item.Id }) |
                    </td>
                </tr>
            }
        }
    </tbody>
</table>
<p>
    <button style="btn-light">
        <a asp-action="CreateSubmissionWriteOffTools">Create Submission</a>
    </button>
</p>